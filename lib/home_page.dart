import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  Widget build(BuildContext context) {
    return Container(
      height: 1000,
      child: Text("I do lots of cool stuff!  Browse around to find out more."),
    );
  }
}

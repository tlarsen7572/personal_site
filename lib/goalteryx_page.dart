import 'package:flutter/material.dart';

class GoAlteryxPage extends StatelessWidget {
  Widget build(BuildContext context) {
    return Container(
      height: 1000,
      child: Text("I built an SDK for Alteryx!  It's awesome and you should try it!"),
    );
  }
}
